setup_the_court_language = {
	if = {
		limit = {
			exists = $CHAR$
			has_dlc_feature = royal_court
		}
		$CHAR$ = {
			set_court_language = $LANGUAGE$
			if = {
				limit = { NOT = { knows_court_language_of = $CHAR$ } }
				learn_court_language_of = $CHAR$
			}
		}
	}
}
#Changes the rite of nicene characters at game start
change_ruler_and_court_rite_by_county_faith = {
	every_county = {
		limit = { this.faith = faith:$NEW_RITE$ }
		if = {
			limit = { this.holder.faith = faith:nicene }
			this.holder = {
				set_character_faith = faith:$NEW_RITE$
				every_child = {
					limit = { this.faith = faith:nicene }
					set_character_faith = faith:$NEW_RITE$
				}
				every_courtier = {
					limit = { this.faith = faith:nicene }
					set_character_faith = faith:$NEW_RITE$
				}
				every_vassal = {
					limit = {
						NOT = { has_government = theocracy_government }
						primary_title.tier = tier_barony
					}
					set_character_faith = faith:$NEW_RITE$
				}
			}
		}
	}
}
change_ruler_and_court_rite_by_kingdom = {
	every_county = {
		limit = { this.kingdom = title:$KINGDOM$ }
		if = {
			limit = { this.holder.faith = faith:nicene }
			this.holder = {
				set_character_faith = faith:$NEW_RITE$
				every_child = {
					limit = { this.faith = faith:nicene }
					set_character_faith = faith:$NEW_RITE$
				}
				every_courtier = {
					limit = { this.faith = faith:nicene }
					set_character_faith = faith:$NEW_RITE$
				}
				every_vassal = {
					limit = {
						NOT = { has_government = theocracy_government }
						primary_title.tier = tier_barony
					}
					set_character_faith = faith:$NEW_RITE$
				}
			}
		}
	}
}
