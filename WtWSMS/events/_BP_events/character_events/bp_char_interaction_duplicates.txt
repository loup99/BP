#### These are all semi-duplicates of vanilla events (e.g., like when we need a near-copy of vanilla interaction events

namespace = bp_char_interaction_duplicate

bp_char_interaction_duplicate.0250 = {
	type = letter_event
	opening = {
		desc = bp_char_interaction_duplicate.0250.opening
	}
	desc = bp_char_interaction_duplicate.0250.desc
		
	sender = scope:actor
	
	trigger = {}

	immediate = {}

	option = {
		name = bp_char_interaction_duplicate.0250.a
	}
}
