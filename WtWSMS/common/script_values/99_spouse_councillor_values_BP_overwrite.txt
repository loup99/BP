##### Changed from Vanilla: Extended to faith doctrine parameter
spouse_default_task_diplomacy_scale = {
	add = diplomacy
	scope:councillor_liege = {
		if = {
			limit = {
				has_perk = loyalty_and_respect_perk
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		if = {
			limit = {
				OR = {
					culture = { has_cultural_parameter = better_spouses }
					faith = { has_doctrine_parameter = better_spouses }
				}
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		divide = 5
		if = {
			limit = {
				exists = scope:has_been_modified # checking if a saved value scope exists rather than rechecking triggers again
			}
			ceiling = yes
		}
		else = {
			floor = yes
		}
	}
	
	min = 1
}

##### Changed from Vanilla: Extended to faith doctrine parameter
spouse_default_task_martial_scale = {
	add = martial
	scope:councillor_liege = {
		if = {
			limit = {
				has_perk = loyalty_and_respect_perk
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		if = {
			limit = {
				OR = {
					culture = { has_cultural_parameter = better_spouses }
					faith = { has_doctrine_parameter = better_spouses }
				}
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		divide = 5
		if = {
			limit = {
				exists = scope:has_been_modified # checking if a saved value scope exists rather than rechecking triggers again
			}
			ceiling = yes
		}
		else = {
			floor = yes
		}
	}
	
	min = 1
}

##### Changed from Vanilla: Extended to faith doctrine parameter
spouse_default_task_intrigue_scale = {
	add = intrigue
	scope:councillor_liege = {
		if = {
			limit = {
				has_perk = loyalty_and_respect_perk
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		if = {
			limit = {
				OR = {
					culture = { has_cultural_parameter = better_spouses }
					faith = { has_doctrine_parameter = better_spouses }
				}
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		divide = 5
		if = {
			limit = {
				exists = scope:has_been_modified # checking if a saved value scope exists rather than rechecking triggers again
			}
			ceiling = yes
		}
		else = {
			floor = yes
		}
	}
	
	min = 1
}

##### Changed from Vanilla: Extended to faith doctrine parameter
spouse_default_task_learning_scale = {
	add = learning
	scope:councillor_liege = {
		if = {
			limit = {
				has_perk = loyalty_and_respect_perk
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		if = {
			limit = {
				OR = {
					culture = { has_cultural_parameter = better_spouses }
					faith = { has_doctrine_parameter = better_spouses }
				}
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		divide = 5
		if = {
			limit = {
				exists = scope:has_been_modified # checking if a saved value scope exists rather than rechecking triggers again
			}
			ceiling = yes
		}
		else = {
			floor = yes
		}
	}
	
	min = 1
}

##### Changed from Vanilla: Extended to faith doctrine parameter
spouse_default_task_stewardship_scale = {
	add = stewardship
	scope:councillor_liege = {
		if = {
			limit = {
				has_perk = loyalty_and_respect_perk
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		if = {
			limit = {
				OR = {
					culture = { has_cultural_parameter = better_spouses }
					faith = { has_doctrine_parameter = better_spouses }
				}
			}
			multiply = 1.25
			save_temporary_value_as = has_been_modified # for performance reasons, to avoid checking trigger again
		}
		divide = 5
		if = {
			limit = {
				exists = scope:has_been_modified # checking if a saved value scope exists rather than rechecking triggers again
			}
			ceiling = yes
		}
		else = {
			floor = yes
		}
	}
	
	min = 1
}