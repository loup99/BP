##### Changed from Vanilla: swapped out first innovation
vassalization_size_limit = {
	value = vassalize_default_size
	scope:attacker = {
		trigger_if = {
			limit = { government_has_flag = government_is_centralized }
			add = vassalize_growth_per_innovation
			add = vassalize_growth_per_innovation
			add = vassalize_growth_per_innovation
			add = vassalize_growth_per_innovation
		}
		trigger_else = {
			if = {
				limit = {
					culture = {
						has_innovation = innovation_ius_ad_bellum
					}
				}
				add = vassalize_growth_per_innovation
			}
			if = {
				limit = {
					culture = {
						has_innovation = innovation_chronicle_writing
					}
				}
				add = vassalize_growth_per_innovation
			}
			if = {
				limit = {
					culture = {
						has_innovation = innovation_divine_right
					}
				}
				add = vassalize_growth_per_innovation
			}
			if = {
				limit = {
					culture = {
						has_innovation = innovation_rightful_ownership
					}
				}
				add = vassalize_growth_per_innovation
			}
		}
	}
}