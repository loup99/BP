##### Changed from Vanilla: Changed default
regional_heresy = {
	default = strict_regional_heresy
	
	default_regional_heresy = {}
	
	strict_regional_heresy = {}
	
	relaxed_regional_heresy = {}
}

##### Changed from Vanilla: Changed default, added new rule
culture_conversion_speed = {
	default = bp_default_culture_conversion_speed
	
	bp_default_culture_conversion_speed = {}
	
	default_culture_conversion_speed = {}
	
	faster_culture_conversion_speed = { flag = blocks_achievements }
	
	significantly_faster_culture_conversion_speed = { flag = blocks_achievements }
	
	significantly_slower_culture_conversion_speed = {}
	
	slower_culture_conversion_speed = {}
}

##### Changed from Vanilla: Changed default, added new rule
faith_conversion_speed = {
	default = bp_default_faith_conversion_speed
	
	bp_default_faith_conversion_speed = {}
	
	default_faith_conversion_speed = {}
	
	faster_faith_conversion_speed = { flag = blocks_achievements }
	
	significantly_faster_faith_conversion_speed = { flag = blocks_achievements }
	
	significantly_slower_faith_conversion_speed = {}
	
	slower_faith_conversion_speed = {}
}

##### Changed from Vanilla: Changed default, added new rule
hybrid_and_divergence_cooldowns = {
	
	categories = {
		tweaks culture
	}
	
	default = bp_default_hybrid_and_divergence_cooldowns

	bp_default_hybrid_and_divergence_cooldowns = {

	}

	default_hybrid_and_divergence_cooldowns = {

	}

	slower_hybrid_and_divergence_cooldowns = {

	}

	no_hybrid_and_divergence_cooldowns = {
		flag = blocks_achievements
	}

	faster_hybrid_and_divergence_cooldowns = {

	}
}