map_table_style_ep3 = {
	dlc_feature = roads_to_power
	environment = "gfx/map/environment/environment_ep3_table.txt"
	priority = 2
	is_shown = {
		##### Changed from Vanilla: Also used by Roman Empire
		OR = {
			trigger_if = {
				limit = { exists = this.culture } # When no character has been selected yet, this needs to be checked so it doesn't ping errors
				culture ?= { has_building_gfx = byzantine_building_gfx }
			}
			trigger_if = {
				limit = { exists = this.government_type }
				has_main_roman_government_trigger = yes
			}
		}
	}
}